# Corrections needed in the submitted report

The attached report was read but not overwritten. Before submission:

1. Replace MongoDB with **PostgreSQL** throughout the WRRC/database discussion and architecture diagram. PostgreSQL matches the uploaded backend example and is allowed by the AB.
2. Complete Q5 using the implemented Users–Items–Claims ERD and SQL constraints; add an actual screenshot of the diagram. `docs/ERD.md` is supplied.
3. Replace the unrelated game-engine table-of-contents entries and game-engine references with the project's correct headings and sources.
4. Add genuine frontend/backend GitHub links and deployed URLs to Q6 once published.
5. Compare the implemented screens with the Figma wireframes; explain any changes instead of claiming exact matching without checking.
6. Notifications here mean status and employee notes shown in the dashboard. No email/SMS notification service was implemented. Do not claim otherwise.
7. Use the Part 2 discussion as a draft, add your real screenshots/tests, and study the code before viva.
8. The AB asks for source ZIPs excluding node_modules and a database schema with inserted data. `schema.sql` includes six clearly labelled example records. If verified real records are required, obtain permitted anonymized records and document their source.
9. The AB lists a declaration form and oral discussion in addition to the report and app. These are not replaced by source code.

The attached AB gives the final submission date as **10 September 2026** and formative assessment through **3 September 2026**. Verify any later instructor announcement separately.
